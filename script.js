import { conversation as conv } from './chat'

console.log(conv);

document.addEventListener() => {
    const target = document.querySelector('. conversation')
}


data.forEach(message, index) => {
    setTimeout() => {
    target.innerHTML += `
    <div class="message message--${message.side}">
        <div message="message_text">
        ${message.text}

        </div>
    </div>
`;
    }, index * 500);

});
})

